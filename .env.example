# ============================================================
# SIGNA DISCORD BOT — Environment Configuration
# Copy to .env and fill in your values. NEVER commit .env.
# ============================================================

# --- Signa API ---
# Get your key from app.getsigna.ai/dashboard/api-keys → "+ New API Key"
# Format: cmts_... (only shown once at creation)
# Rate limit: Founding plan = 60 req/hr · 1,000 req/day
# The hourly scan consumes ~2 calls per ticker (signal + that ticker's OWN GEX)
# plus 1 quota check — e.g. 15 tickers ≈ 31 calls/hr (2N+1).
# The BINDING constraint is NOT the hourly sweep alone, though — it's the
# 09:00–09:15 ET morning window, where three jobs share one 60-min rate window:
#   hourly sweep (2N+1) + pre-market brief (4) + macro outlook (N+4) = 3N+9
# At N=15 that's 54 calls; N=16 → 57 (last safe); N=17 → 60 (throttles).
# Keep the watchlist ≤ ~16 tickers to stay under the 58/hr client cap, leaving
# headroom for /signa and /health slash commands.
SIGNA_API_KEY=cmts_your_key_here

# --- Discord webhooks ---
# Required: fallback for all channels not individually configured.
# Server Settings → Integrations → Webhooks → New Webhook
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/XXXXXXXXX/YYYYYYYYY

# Optional: one webhook per channel for independent routing.
# Any channel left blank falls back to DISCORD_WEBHOOK_URL.
DISCORD_WEBHOOK_SIGNALS=
DISCORD_WEBHOOK_MICRO=
DISCORD_WEBHOOK_MACRO=
DISCORD_WEBHOOK_EARNINGS=
DISCORD_WEBHOOK_FOMC=
DISCORD_WEBHOOK_LOOKUPS=
DISCORD_WEBHOOK_BACKTEST=

# 6-gate CALL output (strict-filter paper-validation feed) → #shardi-setup.
# The hourly scan's 6-gate CALLs post here and are NEVER gated by any flag.
# If unset, those CALL posts are SKIPPED (a warning is logged) — they do NOT
# fall back to #signals. Set this once the #shardi-setup webhook exists.
DISCORD_WEBHOOK_SHARDI=

# Ops alerts: bot startup notice, API auth (401) failures, etc.
# If unset, ops posts fall back to DISCORD_WEBHOOK_SIGNALS with a warning logged.
# Set this once you've created a dedicated #ops channel and webhook.
DISCORD_WEBHOOK_OPS=

# Optional: separate high-conviction alerts channel (Tier 3).
# Falls back to DISCORD_WEBHOOK_URL if blank.
DISCORD_ALERTS_WEBHOOK_URL=

# Optional: test channel webhook for --post-test verification (Step 2 of §8).
# Set this to a #test channel webhook to avoid posting to live channels.
DISCORD_TEST_WEBHOOK_URL=

# --- Discord Gateway Bot (slash commands: /signa, /health) ---
# Create a bot at discord.com/developers/applications → Add Bot → OAuth2
DISCORD_APPLICATION_ID=
DISCORD_BOT_TOKEN=
DISCORD_GUILD_ID=

# --- Watchlist ---
# Comma-separated tickers. The hourly sweep fetches each ticker's OWN GEX
# (2 calls/ticker). The binding rate constraint is the 09:00–09:15 ET morning
# stack — hourly sweep + pre-market brief + macro outlook share one 60-min
# window = 3N+9 calls — NOT the hourly sweep alone (2N+1). So N=16 → 57 calls
# (safe), N=17 → 60 (throttles). Keep the list ≤ ~16.
# Note: tickers with non-alphanumeric characters (e.g. BRK.B) are not supported by the
# /signal endpoint's `sym=` parameter — remap to the broker-style alias or omit.
WATCHLIST=NVDA,AAPL,MSFT,GOOGL,AMZN,SPY,QQQ,META,TSLA

# --- Schedule (all times in ET / America/New_York) ---
# Hourly scan fires every hour Mon–Fri (cron: 0 * * * 1-5). Not configurable.
# Nightly digest fires at DIGEST_HOUR:DIGEST_MINUTE on weekdays.
DIGEST_HOUR=21
DIGEST_MINUTE=30

# Pre-market brief at 09:00 ET (regime + screener)
ENABLE_PREMARKET=true

# Macro outlook (regime + top-5 watchlist) posts to #macro twice daily on a
# 12-hour cadence: 09:15 ET (pre-open) and 21:15 ET. Times are TZ-aware
# (America/New_York, DST-safe) and not configurable via env. Test on demand
# with `node bot.js --post-macro-now --dry-run`.

# --- Signa-driven channels (Phase 2) ---
# Master switch for the new #signals / #micro / #macro channel cycles
# (Signa universe → scan → enrich → basic card). When false (default), the
# crons still register and run the full pipeline every cycle, but cards print
# to stdout instead of posting to Discord and NO JSONL track rows are written —
# so you can review behavior with zero production impact. Set to true to go live.
# Schedules (TZ-aware, weekdays): #signals :02 (9-16 ET) · #micro :30 (9-16 ET)
# · #macro 09:15 & 21:15 ET. Each cycle enriches its universe via /api/v1/signal
# through a shared cache (no /scan), filters, ranks by conviction, posts top 5.
# Test on demand:
#   node bot.js --post-signals-channel-now --dry-run
#   node bot.js --post-micro-channel-now --dry-run
#   node bot.js --post-macro-channel-now --dry-run
SIGNA_CHANNELS_ENABLED=false

# Legacy posts to #signals/#micro/#macro (daily digest, pre-market brief, hourly
# ranked grid, GEX regime updates, 12h macro outlook, regime-change alerts).
# Default false because the Phase 2.5 Signa-driven feeds replace them. Set true
# to restore the old behavior. Does NOT affect the 6-gate CALL output
# (→ DISCORD_WEBHOOK_SHARDI) or ops alerts (→ DISCORD_WEBHOOK_OPS), which are
# never gated.
LEGACY_CHANNEL_POSTS_ENABLED=false

# Shared signal cache TTL (ms) for the channels above. Default 1800000 (30 min).
# Lets #micro reuse #signals' fetches and a re-run within TTL cost zero calls.
# SIGNAL_CACHE_TTL_MS=1800000

# --- Gate 5 / GEX regime (optional) ---
# Neutral band (percent) around the gamma flip within which price is treated as
# "at the flip" (a stable neutral regime) instead of flipping gate 5 each poll.
# Default 0.15 (%). Raise to widen the neutral zone; lower to tighten it.
# GEX_FLIP_NEUTRAL_BAND_PCT=0.15

# --- Paper-validation logging (optional; measurement only, no behavior change) ---
# Appends one JSONL line per CALL + one cycle-summary line per hourly scan, for
# offline analysis. Default ./signa_track.jsonl (bot working dir).
# Production (Railway) uses /data/signa_track.jsonl on a mounted Volume.
TRACK_LOG_PATH=./signa_track.jsonl
